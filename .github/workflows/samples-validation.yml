name: Samples Validation

on:
  push:
    branches: ["main"]
  pull_request:
  workflow_dispatch:

jobs:
  samples:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run sample validation
        run: bash scripts/validate-samples.sh